export enum Tag {
  link = 'a',
  btn = 'button',
}

export enum Page {
  home = 'home',
  layout = 'layout',
  lessons = 'lessons',
  lesson = 'lesson',
  games = 'games',
  game = 'game',
  about = 'about',
}

export enum Lang {
  ru = 'ru',
  en = 'en',
}

export enum Theme {
  light = 'light',
  dark = 'dark',
}

export enum Endpoint {
  users = '/users',
  lessons = '/lessons',
}

export enum Method {
  get = 'get',
  post = 'post',
  put = 'put',
  delete = 'delete',
}